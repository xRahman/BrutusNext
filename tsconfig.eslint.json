/*
  Part of BrutusNext

  Fake tsconfig file specifying what eslint should lint
*/

/*
  Eslint reports error if it finds a file that is not included
  in any project. This fake tsconfig file works around it by
  including all .ts files in Src directory. This file has no
  other purpose.
*/

{
  "include":
  [
    "**/Src/**/*.ts"
  ]
}