{
  "compilerOptions":
  {
    "target": "es6",
    "module": "commonjs",
    "strict": true,   // Enforce strict checking of 'null' and 'undefined'.
    "charset": "UTF-8",
    "moduleResolution": "node",
    "newLine": "LF",
    "noEmitOnError": true,
    "outDir": "../../build/server/js",
    ///"outDir": "build/server/js",
    ///"rootDir": "../",
    "rootDir": "../",
    "sourceMap": true,
    "sourceRoot": "../"
    // 'compileOnSave' actually works in vs code if --watch parameter
    //  is passed to 'tsc', but the same functionality is turned on by
    // '"isWatching": true' in tasks.json. We do it there.
    /* "compileOnSave": true*/
  }//,
  /*"files":
  [
    "./src/headers/node.d.ts",
    "./src/game/DynamicClasses.ts",
    "./src/BrutusNext.ts"
  ],*/
  /*
  "exclude": [
        ".git",
        ".vscode",
        "documentation",
        "node_modules",
        "client",
        "server",
        "tiny web server"
    ]
  */
}
